#!/bin/sh

branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$branch" = "main" ] || [ "$branch" = "master" ]; then
  echo "Push blocked: direct pushes to $branch are not allowed."
  echo "Create a feature branch and open a PR to merge into main."
  exit 1
fi

exit 0
